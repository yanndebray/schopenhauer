{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Schopenhauer Will Specification",
  "description": "Configuration for the Schopenhauer document generator in Markdown frontmatter.",
  "type": "object",
  "properties": {
    "will": {
      "type": "object",
      "properties": {
        "template": {
          "type": "string",
          "description": "Template name or path to a .docx reference document."
        },
        "data": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "description": "Path to a data file (CSV, YAML, JSON) relative to the source."
          },
          "description": "External data sources to load and bind to the document."
        },
        "variables": {
          "type": "object",
          "description": "Static variables for template substitution."
        },
        "backend": {
          "type": "string",
          "enum": ["pandoc", "legacy"],
          "default": "pandoc"
        }
      }
    }
  }
}
